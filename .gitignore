# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Images the admin uploads while writing questions — per-machine content, not source
server/uploads

# Claude Code (internal notes, not pushed to GitHub)
CLAUDE.md
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Quizzes written in the admin page — event content, not source
server/quizzes.json

# The admin password hash, written by the first-run screen — per-machine secret
.env
.env.local
